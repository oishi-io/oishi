<%= simple_form_for(@tag, remote: true, method: 'post') do |f| %>
  <%= f.input :name, label: false, placeholder: "Nom du tag" %>
  <%= f.hidden_field :recipe_id, value: @recipe.id %>
  <%= f.submit "Ajouter", class: "btn btn-primary" %>
<% end %>
